site_name: tdfpy
site_description: A Python library for parsing Bruker timsTOF data with centroiding and noise filtering.
repo_url: https://github.com/tacular-omics/tdfpy
repo_name: tacular-omics/tdfpy
theme:
  name: material
  palette:
    scheme: slate
    primary: indigo
  features:
    - navigation.sections
    - navigation.top
    - content.code.copy
    - search.highlight

nav:
  - Home: index.md
  - Getting Started: getting-started.md
  - API Reference:
    - Reader: api/reader.md
    - Data Classes: api/elems.md
    - Lookups: api/lookup.md
    - Centroiding: api/centroiding.md
    - Low-Level: api/low-level.md

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [src]
          options:
            docstring_style: google
            show_source: true
            show_root_heading: true
            show_symbol_type_heading: true
            members_order: source
            inherited_members: true
            merge_init_into_class: true
            filters: ["!^_"]
            show_signature_annotations: true
            separate_signature: true
            annotations_path: source
            signature_crossrefs: true

markdown_extensions:
  - pymdownx.highlight
  - pymdownx.superfences
  - admonition
